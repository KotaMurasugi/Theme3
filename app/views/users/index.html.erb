<%= render 'user', user: @user, book: @book %>
<h1>Users</h1>
<% @users.each do |user| %>
  <%= link_to user_path(user.id) do %>
  <%= image_tag user.get_profile_image(100,100) %>
  <%= user.name %>
  <%= link_to 'Show', user %>
  <% end %>
<% end %>